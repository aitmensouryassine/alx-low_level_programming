#include "main.h"
/**
 * add - add two number
 * @a: first integer of the addition operation
 * @b: second integer of the addition operation
 * Return: the sum (integer) of the two integers
 */
int add(int a, int b)
{
	return (a + b);
}
